## ¿Como volcar estos scripts en tu SGBD 🐬?

Hay que abrir la terminal en este directorio (`scripts\bd\MySQL\`) y escriban en la terminal

```bash
mysql -u tuUsuarioMySQL -p
```

y luego escribir el comando

```bash
mysql> SOURCE install.sql
```

Y con eso corren los scripts en su SGBD 🐬 ✌🏽